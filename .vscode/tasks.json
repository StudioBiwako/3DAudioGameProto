{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build", // Task name
      "type": "shell", // Use shell commands
      "command": "cmake --build build", // CMake build command
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Build the entire project using CMake"
    }
  ]
}
